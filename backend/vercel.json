{
    "builds": [
      {
        "src": "backend/package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npm run build && npx prisma generate"
        }
      }
    ]
  }
  